# Tesseract 产品需求文档（PRD）

| 项目       | 内容                                       |
| ---------- | ------------------------------------------ |
| 产品名称   | Tesseract                                  |
| 文档版本   | v0.7                                       |
| 一句话定位 | 研究员的**观察名单** + **可推敲的证据型调查**。 |

> **Slogan：** "Noise is the enemy of truth."

---

## 1. 已实现

1. **观察名单**：录入公司名称 → **开始深入调查**。
2. **调查工作台**（`/company/:id`）：五阶段 Tab、调研 Loop（定向→收集→推敲→沉淀）、证据卡片 + 引用。
3. **信源库**（`/sources`）：人类策展优质网址/域名。

技术：Vue 3 + TS + Vite + Element Plus + IndexedDB v5。

---

## 2. 核心概念

| 概念     | 说明 |
| -------- | ---- |
| 观察名单 | 用户关注的标的公司列表 |
| 在名单上 | `on_list` |
| 调查中   | `investigating`；详情页将承载调研循环（Phase 2） |

---

## 3. Phase 2 方向（已设计，待开发）

**目标**：AI 辅助执行「收集」，人类与 AI 在 **Loop** 中反复推敲；**只沉淀关键结论 + 引用 URL**；人类可积累**优质信源库**。

完整设计见：**[docs/RESEARCH_DESIGN.md](./docs/RESEARCH_DESIGN.md)**

摘要：

### 3.1 五阶段 Tab

1. **坐标系** — 公司与行业地图  
2. **机理** — 供需与利润来源  
3. **约束** — 关键限制因素与发展因素（核心）  
4. **系统** — 上下游、竞争、政策与治理  
5. **总结** — 本质总结（区分事实/推断/未知）

### 3.2 单阶段内调研循环

```
定向 → 收集 → 推敲 → 沉淀（证据卡片）
         ↑__________________|
              （未过关则再来）
```

- **沉淀物**：证据卡片（短主张 + 认知类型 + 摘要 + 引用列表）  
- **推敲**：可证伪、有引用、AI 提反证、可修订/取代旧卡片  

### 3.3 三类持久化数据（规划）

| Store | 用途 |
| ----- | ---- |
| `evidence` | 关键结论卡片 |
| `citations` | URL、标题、访问日、可选摘录 |
| `trusted_sources` | 人类策展的优质网址/域名，供后续调查复用 |

### 3.4 AI 原则

- 不讨好、不编造未访问来源的数据  
- 收集步可推荐检索与 URL；沉淀需人类确认或满足引用规则  
- 发现优质站点 → 建议人类加入信源库  

---

## 4. 页面路由（规划）

| 路由 | 状态 |
| ---- | ---- |
| `/` | ✅ 观察名单 |
| `/company/:id` | ✅ 调查工作台 |
| `/sources` | ✅ 信源库 |

---

## 5. 数据模型（当前 MVP）

**companies**

| 字段 | 说明 |
| ---- | ---- |
| id | 主键 |
| name | 公司名称 |
| status | `on_list` \| `investigating` |
| createdAt / updatedAt | ISO |

Phase 2 字段与 `evidence` / `citations` / `trusted_sources` 见 [RESEARCH_DESIGN.md](./docs/RESEARCH_DESIGN.md)。

---

## 6. 非目标

- 投资建议、买卖结论  
- 实时行情  
- 囤积全文/原始网页（仅关键信息 + 链接）  
- 无引用的「结论型」AI 输出  

---

## 7. 协作与合并

见 [AGENTS.md](./AGENTS.md)：Agent 完成改动后**直接合并 PR 至 main**。
