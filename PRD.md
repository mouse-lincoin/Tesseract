# Tesseract 产品需求文档（PRD）

| 项目         | 内容                                                         |
| ------------ | ------------------------------------------------------------ |
| 产品名称     | Tesseract（商业透视引擎）                                     |
| 文档版本     | v0.3（Draft）                                                |
| 状态         | 概念验证 / 待评审                                             |
| 一句话定位   | 一个基于"第一性原理"的商业验证引擎：只回答"这家公司跨过它的生死线了吗？" |
| 首发范围     | 市场：A 股 / 港股；行业：SaaS / 企业软件；形态：C 端工具      |
| 适用读者     | 产品、研发、数据工程、设计                                    |

> **Slogan：** "Noise is the enemy of truth."（噪音是真相的敌人）

---

## 1. 产品理念（Product Philosophy）

Tesseract 不是一个大而全的数据终端，而是一个基于**第一性原理**构建的商业透视引擎。

我们相信：**每个行业都有其不可违背的"物理定律"（关键限制因素），而绝大多数商业分析的失败，都源于在 100 个次要指标里迷失，却忽略了那唯一决定生死的变量。**

Tesseract 存在的唯一目的，就是：

1. **识别**某个行业的唯一关键限制（The One Rule / 生死线）；
2. 用数据**无情地验证**目标企业是否突破了这一限制。

没有突破，一切繁荣皆是虚妄；一旦突破，即迎来指数级增长。

> **数据观（重要）：** 数据本身不是目的。一切指标、代理指标都只是**辅助手段**，最终都要服务于唯一目标——推导并验证"关键限制因素"是否被突破。我们宁可少看 99 个指标，也要把那 1 个本质问题看透。

---

## 2. 问题陈述（Why Tesseract）

| 现状痛点                         | 说明                                                                 |
| -------------------------------- | -------------------------------------------------------------------- |
| 信息过载                         | 传统金融终端（Bloomberg、Wind、同花顺）提供成百上千个指标，用户在噪音中迷失，难以抓住主要矛盾。 |
| 缺乏"行业语境"                   | 同一个指标（如毛利率）在不同行业含义完全不同，通用打分模型抹平了行业差异。 |
| 结论模糊                         | 大多数报告给出"建议关注/中性/偏多"等模棱两可的结论，无法支撑决策。   |
| 数据冗余采集成本高               | 全量抓取数据既昂贵又低效，90% 的数据对当前判断没有价值。             |

**Tesseract 的反向解法：** 先定义"什么最重要"，再只为这一件事采集数据，最终只给出一个二元结论。

---

## 3. 目标用户与画像（Target Users）

> **首发市场聚焦 A 股 / 港股**，下列用户画像均以这两个市场的标的为主要分析对象。

| 用户类型       | 画像与诉求                                                     |
| -------------- | -------------------------------------------------------------- |
| 进阶个人投资者 | 厌倦了被海量指标淹没，想要"一针见血"的判断辅助（**MVP 主力人群**）。 |
| 二级市场研究员 | 在财报季快速判断一家公司的核心逻辑是否被验证或证伪。          |
| 行业战略 / BD  | 评估竞品或潜在合作方是否已建立起真正的护城河。                |
| 一级市场投资人 | 评估成长期公司是否具备指数增长的结构性拐点。                  |

> **非目标用户：** 需要完整财务报表、合规审计、量化高频交易信号的专业机构（这些不是本产品要解决的问题）。

---

## 4. 产品目标与非目标（Goals / Non-Goals）

### 4.1 目标（Goals）

- 用户**仅输入公司名称或股票代码**，即可得到该公司的"生死线判决"。
- 首发覆盖 **A 股 / 港股**市场的 **SaaS / 企业软件**行业，跑通完整链路后再横向扩展。
- 系统能为主流行业内置"唯一关键限制法则"，并支持持续扩充。
- 在无法直接获取核心指标时，能通过**代理指标（Proxy Metrics）逆向推算**。
- 输出极简、明确、可解释：一句话结论 + 一张图。
- 产品形态为 **C 端工具**，MVP 阶段**完全免费、不设付费墙**。

### 4.2 非目标（Non-Goals）

- ❌ 不做全量财务报表展示与下载。
- ❌ 不做投资建议 / 买卖信号（仅做"限制突破与否"的事实判定）。
- ❌ MVP 阶段不覆盖美股及冷门行业（先深后广）。
- ❌ MVP 阶段不做实时行情与盘中数据。
- ❌ MVP 阶段不做账号体系与付费墙（后续视情况再引入）。

---

## 5. 核心用户场景（User Flow）

```
输入  →  识别  →  匹配  →  采集  →  验证  →  判决
```

1. **输入：** 用户仅需输入"公司名称"或"股票代码"（A 股 / 港股）。
2. **识别：** 系统识别其核心业务，归类到对应行业（可命中一个或多个）。
3. **匹配：** 从「行业公理库」中匹配该行业的致命限制及验证指标。
4. **采集：** 触发「狙击式数据采集器」定向抓取核心指标或代理指标。
5. **验证：** 「破壁验证引擎」拉取 3–5 年时序数据，判断趋势是否脱离"行业诅咒"。
6. **判决：** 输出唯一结论——`未突破 (Restricted)` 或 `已破壁 (Wall-Broken)`，并附一句话理由；若命中多条生死线，则逐条判决并汇总。

> **关键体验原则：** 黑盒处理。用户不需要理解中间过程，只为最终的"判决书"买单。

---

## 6. 核心功能模块（Core Modules）

### 6.1 行业公理库（The Axiom Dictionary）

放弃全盘抓取，系统内部预设各大行业的**唯一关键限制法则（The One Rule）**。

| 行业           | 核心限制（生死线） | 验证指标                                    | 破壁阈值（示例）         |
| -------------- | ------------------ | ------------------------------------------- | ------------------------ |
| SaaS / 企业软件 | 客户流失           | NDR（净收入留存率）                         | NDR > 115%               |
| 消费品品牌      | 流量反噬           | 营销费用率趋势 + 毛利率稳定性               | 营销费用率持续下降且毛利率企稳 |
| 高端制造        | 技术内卷           | 核心技术/专利驱动的新品营收占比             | 新品营收占比 > 30%       |

> 阈值为可配置项，每条规则需包含：`行业定义`、`核心限制描述`、`验证指标`、`数据来源`、`破壁阈值`、`代理指标回退方案`。

**需求：**
- 规则以结构化配置（如 JSON / YAML）存储，便于非研发人员维护与扩充。
- **MVP 首发行业为 SaaS / 企业软件**，先把这一条规则打磨到可信，再横向扩展。
- **支持一家公司命中多条生死线（复合行业）：** 当目标公司横跨多业务（如同时做硬件 + SaaS），系统按命中的每条规则分别验证并出具子判决，最终汇总为总判决（任一核心限制未突破即整体高危）。

### 6.2 狙击式数据采集器（Sniper Scraper）

**绝不抓取无用数据。** 根据 6.1 确定的验证指标，定向寻找数据。数据始终是"辅助"，目标是推导出关键限制是否被突破。

- **数据源策略：** 优先接入**第三方免费数据 API**（如公开财报接口、交易所披露、开源财经数据源），降低成本与合规风险；不足部分再以定向抓取补齐。
- **直接指标优先：** 若财报/招股书直接披露核心指标，直接采集对应字段。
- **代理指标回退：** 若未直接披露（如未公布 NDR），则抓取**预收账款变化、合同负债、营销费用转化率**等代理指标进行逆向推算。
- **来源可追溯：** 每个数据点需记录来源（财报章节 / 公告 / 第三方 API），保证结论可审计。

### 6.3 破壁验证引擎（Wallbreaker Engine）

- **时序验证：** 单点数据无意义。引擎必须拉取过去 **3–5 年**（或 8–12 个季度）的指标数据。
- **逻辑判定：** 判断指标趋势是否与"行业诅咒"脱钩。
- **终局打分：** 每条生死线只有两个结果——`未突破 (Restricted)` 或 `已破壁 (Wall-Broken)`。
- **多规则汇总：** 复合行业公司逐条出子判决；只要存在任一"未突破"，总判决即为高危/未破壁。
- **可解释性：** 输出判定所依据的关键数据与趋势描述，避免"黑箱不可信"。

---

## 7. UI / UX 设计原则（极简界面）

- **黑客帝国风格：** 深色背景，绿色/单色高对比数据线。
- **单一视觉焦点：** 屏幕中央只有两条线——
  - 一条**基准线**（行业生死基准线 / 破壁阈值）；
  - 一条**企业曲线**（关键代理指标的时序走势）。
- **判决书：** 图表下方用极简的一句话输出结论。
- **复合行业展示：** 若公司命中多条生死线，以可切换的卡片/标签分别呈现每条判决，并在顶部给出总判决。

> **判决书示例：**
> "该企业所处消费赛道，本质限制为流量成本。系统验证其近 12 个季度营销费用率从 45% 升至 62%，并未突破品牌护城河限制，判定为：**高危 / 未破壁**。"

---

## 8. 技术架构概览（High-Level Architecture）

三个极简服务串联成一条流水线（首发数据来源以第三方免费 API 为主）：

```
        ┌─────────────────┐
用户输入 │  公司名 / 代码   │  （A 股 / 港股）
        └────────┬────────┘
                 ▼
        ┌─────────────────┐     下发"该验证什么"（可下发多条规则）
        │ Axiom RuleBase  │────────────────────┐
        │   行业公理库     │                    ▼
        └─────────────────┘          ┌──────────────────┐
                                      │   Data Sniper    │  第三方免费 API 优先
                                      │   数据狙击手      │  定向抓取核心/代理指标
                                      └────────┬─────────┘
                                               ▼
                                      ┌──────────────────┐
                                      │ Verification Core│  时序趋势 + 破壁判定
                                      │   验证内核        │  多规则逐条判决并汇总
                                      └────────┬─────────┘
                                               ▼
                                      ┌──────────────────┐
                                      │     判决书 UI     │  一句话结论 + 一张图
                                      └──────────────────┘
```

---

## 9. MVP 范围与迭代路线（Roadmap）

### Phase 1 — MVP（验证产品理念）
- 市场：A 股 / 港股；行业：**SaaS / 企业软件**（内置 1 条公理规则）。
- 数据：优先接入第三方免费数据 API，跑通"输入 → 判决"主链路。
- 完成判决书 UI 原型（一图 + 一句话），C 端、免费、无付费墙。

### Phase 2 — 扩品类 + 自动采集
- 公理库扩充至 3–5 个行业，支持复合行业多规则判决。
- Data Sniper 实现自动定向抓取与代理指标推算。

### Phase 3 — 规模化与商业化探索
- 公理库可视化维护后台。
- 视用户规模探索付费能力（高级判决/批量查询等）。
- 评估是否扩展至美股等更多市场。

---

## 10. 成功指标（Success Metrics）

| 维度       | 指标                                                       |
| ---------- | ---------------------------------------------------------- |
| 准确性     | 判决结论与事后实际表现的吻合率（人工抽样回测）。           |
| 效率       | 单次查询从输入到判决的平均耗时。                           |
| 覆盖度     | 公理库覆盖的行业数量、可查询公司数量。                     |
| 用户价值   | 用户留存率、单用户查询频次、"判决书"被分享/引用次数。      |

---

## 11. 风险、假设与已确认决策

### 风险与假设
- **数据可得性：** 第三方免费 API 的字段覆盖与更新频率有限，代理指标推算的准确度依赖于公开披露质量，可能存在偏差。
- **规则有效性：** "唯一关键限制"的假设在某些复合型商业模式下可能不成立（已通过"多条生死线并存"机制部分缓解）。
- **合规风险：** 数据获取需遵守各数据源的服务条款与当地法规。

### 已确认决策（v0.3）

| #   | 议题         | 决策                                                                                   |
| --- | ------------ | -------------------------------------------------------------------------------------- |
| 1   | 目标市场     | **首发聚焦 A 股 / 港股**；美股暂不纳入 MVP。                                            |
| 2   | MVP 首发行业 | **SaaS / 企业软件**（指标定义最清晰）。                                                 |
| 3   | 数据获取方式 | 优先接入**第三方免费数据 API**；数据仅为辅助，最终落脚点始终是"关键限制因素"的推导验证。 |
| 4   | 商业模式     | **C 端工具**；目前**不设付费墙**，后续可能引入付费能力。                                |
| 5   | 复合行业处理 | **允许多条生死线并存**：横跨多业务的公司可命中多条规则，分别判决并汇总。                |

> 上述决策已在本文档对应章节落地（见 §3、§4、§6、§8、§9）。后续若有调整，以此表为准并同步更新正文。
