# Tesseract 产品需求文档（PRD）

| 项目       | 内容                                                         |
| ---------- | ------------------------------------------------------------ |
| 产品名称   | Tesseract（关键限制因素推演引擎）                            |
| 文档版本   | v0.5（Draft）                                               |
| 一句话定位 | 找到一家公司的**关键限制因素**，并预测它能否被解决、解决到什么程度。 |
| 首发范围   | 市场：A 股 / 港股；行业：SaaS / 企业软件；形态：**纯前端 C 端工具**（Vue 3 + Element Plus + Vite 8，数据存 IndexedDB） |

> **Slogan：** "Noise is the enemy of truth."（噪音是真相的敌人）

---

## 1. 产品理念

每个商业模式都有且仅有一个**关键限制因素**（生死线）。Tesseract 的全部价值就在于两步：

1. **推导出这个关键限制因素**——这是产品的核心与重点；
2. **基于数据预测它能否被解决、当前解决到什么程度**。

我们**不下判决、不给结论性结果，只提供预测**。一切数据都只是辅助手段，最终都服务于"识别关键限制因素 + 判断其解决前景"这一件事。宁可少看 99 个指标，也要把那 1 个关键限制因素看透。

---

## 2. 它解决什么问题

传统金融工具给你上百个指标，让你淹没在噪音里，且用通用模型抹平了行业差异，结论往往模糊。

Tesseract 反向解题：**先锁定唯一的关键限制因素，只为它采集数据，再输出对其解决前景的预测。**

---

## 3. 核心流程

```
输入公司名/代码  →  推导关键限制因素  →  定向采集数据  →  预测解决前景
```

1. **输入：** 用户仅输入公司名称或股票代码（A 股 / 港股）。
2. **推导关键限制因素：** 系统识别其核心业务，从公理库匹配该行业的关键限制因素（可命中一个或多个）。**这是最重要的一步。**
3. **采集：** 围绕该关键限制因素定向采集数据（直接指标或代理指标），其余一概不取。
4. **预测：** 基于时序趋势，预测这个关键限制因素能否被解决、当前解决进展如何。

> 用户不需要理解中间过程，只需要知道："它的关键限制因素是什么？它正在被解决吗？"

---

## 4. 核心模块

### 4.1 公理库（关键限制因素字典）

存储各行业**唯一的关键限制因素**及其推导/判断逻辑。

| 行业            | 关键限制因素 | 观察用代理指标                |
| --------------- | ------------ | ----------------------------- |
| SaaS / 企业软件 | 客户流失     | NDR（净收入留存率）           |
| 消费品品牌      | 流量成本     | 营销费用率趋势 + 毛利率稳定性 |
| 高端制造        | 技术内卷     | 核心技术/专利新品营收占比     |

- 规则以结构化配置（JSON / YAML）存储，便于扩充。
- **MVP 首发 SaaS / 企业软件**，先打磨一条规则再横向扩展。
- **支持复合行业**：一家公司可命中多条关键限制因素，分别推演。

### 4.2 数据狙击手（Sniper Scraper）

只为推导和印证关键限制因素而采集数据，**绝不抓取多余数据**。

- **MVP（纯前端）：** 受浏览器 CORS 限制，暂不直连第三方 API；以**内置示例数据 + 手动录入 + JSON 导入/导出**为主。
- **后续：** 接入后端/代理后再实现第三方免费 API 自动采集与代理指标推算。
- **可追溯：** 每个数据点记录来源，保证预测可解释。

### 4.3 预测推演引擎（Forecast Core）

- **时序为基础：** 拉取过去 3–5 年（8–12 个季度）的指标数据，单点数据无意义。
- **输出预测而非判决：** 描述关键限制因素的解决趋势与前景，例如「正在缓解 / 基本持平 / 持续恶化」，以及「短期内能否被解决」的预测。
- **不给结论性结果：** 不输出"达标/不达标""买入/卖出"这类二元判决。
- **复合行业：** 逐条关键限制因素分别给出预测。

---

## 5. 输出与界面（极简 UI）

深色极简界面，每次查询只呈现三样东西：

1. **关键限制因素：** 一句话点明这家公司的生死线是什么。
2. **一张趋势图：** 关键限制因素的代理指标时序走势 +（可选）参考线。
3. **一句预测：** 对其解决前景的判断。

> **示例：** "金蝶国际的关键限制因素是 SaaS 的客户流失。近 5 年 NDR 从 105% 缓慢爬升至 113%，**正在改善但仍未稳固**，预测短期内这一限制尚难被彻底解决。"

复合行业以可切换卡片分别呈现每条关键限制因素及其预测。

---

## 6. 已确认决策与范围

| 议题     | 决策                                                       |
| -------- | ---------------------------------------------------------- |
| 目标市场 | 首发 A 股 / 港股；美股暂不纳入 MVP                          |
| 首发行业 | SaaS / 企业软件                                            |
| 技术形态 | **纯前端单页应用，无后端**；数据本地存 IndexedDB           |
| 商业模式 | C 端工具，MVP 免费、不设付费墙，后续可能引入付费           |
| 复合行业 | 允许一家公司命中多条关键限制因素，分别推演                  |

**非目标：** 后端服务、完整财务报表、投资建议、实时行情、账号体系与付费墙（均不在 MVP 内）。

---

## 7. 技术框架与项目脚手架（MVP）

> 本节供工程实现直接据此搭建项目。MVP 为**纯前端单页应用，不含后端**，所有数据存储于浏览器 **IndexedDB**。

### 7.1 技术栈

| 类别       | 选型                                  | 说明                                   |
| ---------- | ------------------------------------- | -------------------------------------- |
| 构建工具   | **Vite 8**                            | 开发服务器与打包                       |
| 框架       | **Vue 3**（Composition API，`<script setup>`） | 单页应用                          |
| 语言       | **TypeScript**                        | 全量 TS                                |
| UI 组件库  | **Element Plus**                      | 深色主题                               |
| 状态管理   | **Pinia**                             | 全局状态                               |
| 路由       | **Vue Router**                        | 页面路由                               |
| 图表       | **ECharts**（建议 `vue-echarts` 封装）| 关键限制因素的趋势图                   |
| 本地存储   | **IndexedDB**（独立封装层，见 §7.3）  | 全部业务数据持久化                     |
| 工程规范   | ESLint + Prettier（建议）             | 统一代码风格                           |

> **为什么纯前端 + IndexedDB：** MVP 无需后端、零部署成本、数据留在本地、隐私友好。代价是受浏览器 CORS 限制，自动数据采集需待后续接入后端/代理（见 §4.2）。

### 7.2 建议目录结构

```
tesseract/
├── index.html
├── vite.config.ts
├── tsconfig.json
├── package.json
├── src/
│   ├── main.ts
│   ├── App.vue
│   ├── router/                # 路由
│   ├── stores/                # Pinia 状态
│   ├── views/                 # 页面：分析页 / 规则管理页 / 数据录入页
│   ├── components/            # 通用组件（趋势图、预测卡片等）
│   ├── modules/
│   │   ├── axiom/             # 公理库：关键限制因素规则的读写与匹配
│   │   ├── sniper/            # 数据录入 / 导入（MVP 手动）
│   │   └── forecast/          # 预测推演：时序趋势 + 解决前景
│   ├── db/                    # ★ IndexedDB 独立封装层（与业务解耦，见 §7.3）
│   │   ├── index.ts           # 数据库实例、版本与升级（onupgradeneeded）
│   │   ├── schema.ts          # 对象仓库（store）与索引定义
│   │   ├── client.ts          # 底层 CRUD（Promise 封装）
│   │   └── repositories/      # 面向业务的仓储：rules / companies / metrics / analyses
│   ├── types/                 # 全局类型定义
│   └── assets/
└── README.md
```

### 7.3 IndexedDB 封装要求（重点：必须独立解耦）

- **独立模块：** IndexedDB 全部逻辑收敛在 `src/db/`，业务层**只通过仓储（Repository）接口**访问，**严禁在组件/store 里直接调用 IndexedDB 原生 API**。
- **统一异步接口：** 对外暴露基于 `Promise / async` 的方法，屏蔽事务、游标、版本升级等底层细节。
- **可替换：** 接口与实现分离（如定义 `StorageClient` 抽象），未来可无痛替换为后端 API 或其他存储，而**不改动业务代码**。
- **版本迁移：** 在 `onupgradeneeded` 中集中管理对象仓库的创建与索引升级。
- **导入/导出：** 提供全库数据的 JSON 导入/导出能力，便于备份与无后端下的数据流转。
- **通用仓储接口示意（伪代码）：**

```ts
interface Repository<T> {
  get(id: string): Promise<T | undefined>;
  getAll(): Promise<T[]>;
  query(index: string, value: IDBValidKey): Promise<T[]>;
  put(item: T): Promise<string>;
  delete(id: string): Promise<void>;
}
```

> 实现可基于原生 IndexedDB 自封装，或选用 `idb` 这类轻量库；无论选型如何，业务层只能看到仓储接口。

### 7.4 数据模型（IndexedDB Object Stores）

| Store        | 含义           | 关键字段（示意）                                                        |
| ------------ | -------------- | ----------------------------------------------------------------------- |
| `rules`      | 公理库规则     | `id, industry, constraintName(关键限制因素), proxyMetric, threshold?, description` |
| `companies`  | 公司           | `id, name, code(如 0268.HK), market(A股/港股), industryIds[]`           |
| `metrics`    | 时序数据       | `id, companyId, metricKey, points:[{period, value}], source`            |
| `analyses`   | 预测结果       | `id, companyId, ruleId, trend(改善/持平/恶化), prediction(文本), generatedAt` |
| `settings`   | 配置 (KV)      | `key, value`                                                            |

### 7.5 关键页面（MVP）

- **分析页：** 输入公司名/代码 → 展示关键限制因素 + 趋势图 + 一句预测。
- **规则管理页：** 查看/编辑公理库的关键限制因素规则（对 JSON 友好）。
- **数据录入/导入页：** 手动录入或导入公司指标时序数据；支持 JSON 导入/导出。

### 7.6 初始化与种子数据

首次启动时，将内置的 **SaaS 关键限制因素规则**与一份**示例公司数据**写入 IndexedDB，保证开箱即用、可直接演示主链路。

---

## 8. 路线图

- **Phase 1 — MVP：** 纯前端脚手架（§7）跑通；A 股/港股 + SaaS，内置 1 条关键限制因素规则与示例数据，跑通"输入 → 推导 → 预测"主链路 + 极简 UI（C 端免费）。
- **Phase 2 — 扩行业：** 公理库扩充至 3–5 个行业，支持复合行业多限制推演；接入后端/代理实现自动采集。
- **Phase 3 — 规模化：** 公理库可视化维护后台，探索付费能力，评估扩展更多市场。

---

## 9. 风险与假设

- **数据可得性：** 纯前端 MVP 依赖手动录入/导入；后续自动采集受 API 字段覆盖与 CORS 限制，代理指标推算可能存在偏差。
- **关键限制因素假设：** "唯一关键限制"在复合业务下可能不成立（已用"多条并存"机制部分缓解）。
- **合规：** 数据获取需遵守各数据源条款与当地法规。
